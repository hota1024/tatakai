import { Lobby } from '@/page-components/Lobby/Lobby'
import { NextPage } from 'next'

/**
 * Lobby page.
 */
export const LobbyPage: NextPage = () => {
  return <Lobby />
}

export default LobbyPage
